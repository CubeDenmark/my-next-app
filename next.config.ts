import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  images: {
    domains: ['via.placeholder.com', 'img.icons8.com', 'api.uifaces.co', 'randomuser.me'],
  },
};

export default nextConfig;
